# Core dependencies
mcp==1.26.0              # MCP SDK for server/tool registration
httpx==0.28.1            # Async HTTP client for Ollama API calls
pydantic==2.12.5         # Data validation (required by mcp)

# Optional: vector memory (install with pip install mcplex[memory])
# chromadb==1.5.2        # ChromaDB vector database

# Dev dependencies (install with pip install mcplex[dev])
# pytest==9.0.2          # Test framework
# pytest-asyncio==1.3.0  # Async test support
